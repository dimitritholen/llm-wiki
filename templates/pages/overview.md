# Overview

This wiki has no ingested sources yet. The agent rewrites this page as the
wiki grows: what the vault is about, the main threads, and the open
questions the sources raise but do not answer.

## Main threads

None yet. After the first ingests, list the central concept and entity pages
here as wikilinks with one line each.

## Open questions

None yet.

## How to navigate

Start from `index.md`. Each directory has its own index with one line per
page. `disputes.md` lists every unresolved contradiction.
